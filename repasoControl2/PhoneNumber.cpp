#include <iomanip>
#include <iostream>
#include <string>


#include "PhoneNumber.h"

explicit PhoneNumber(std::string areaC, std::string exch, std::string ln) : areaCode(area), exchange(exch), line(ln) {
    // areaCode = area;
    // exchange = exch;
    // line = ln;
}

